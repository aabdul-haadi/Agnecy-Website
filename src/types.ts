export type Project = {
  title: string;
  description: string;
  tools: string[];
  image: string;
  link?: string;
  github?: string;
};

export type Service = {
  title: string;
  description: string;
  icon: string;
};